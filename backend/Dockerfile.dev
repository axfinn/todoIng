# 使用官方Node.js运行时作为父镜像
FROM node:16

# 设置工作目录
WORKDIR /app

# 复制package.json和package-lock.json（如果存在）
COPY package*.json ./

# 安装依赖
RUN npm install

# 复制其余文件
COPY . .

# 暴露端口
EXPOSE 5001

# 启动开发服务器
CMD ["npm", "run", "dev"]